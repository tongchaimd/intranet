header.navbar
	.navbar-brand
		a.navbar-item(href="#")
			img.image(src="/public/images/dipt.png" style="height: 30px")
		a.navbar-item(href=paths.news()) News
		a.navbar-item(href=paths.businessCards()) Business Cards
		.navbar-burger(onclick="document.querySelector('.navbar-menu').classList.toggle('is-active')")
			span
			span
			span
	.navbar-menu
		.navbar-end
			a.navbar-item(href=paths.users(currentUser)) #{currentUser.username}
			a#sign-out.navbar-item.has-text-danger(data-target=paths.sessions()) Sign Out